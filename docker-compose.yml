services:
  napcat:
    container_name: napcat
    image: mlikiowa/napcat-docker:v4.4.4

  twitter-subscriber:
    build:
      context: .
    restart: always
    volumes:
      - type: bind
        source: /var/lib/arona
        target: /store
