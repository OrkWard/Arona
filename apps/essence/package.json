{
  "name": "essence",
  "type": "module",
  "scripts": {
    "dev": "tsc --build --watch",
    "main": "node --env-file .env ./dist/main.js",
    "config": "op inject --in-file example.env --out-file .env -f"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "catalog:",
    "@henrygd/queue": "^1.2.0",
    "es-toolkit": "catalog:",
    "onebot": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
